#include <stdio.h>
int main(){
    int i,n,arr[];
    for(i=0;i<n,i++){
        scanf("%d",a[i]);
    }
    if(a[i]>a[i-1]){
        printf("Sorted");
    }
    else{
        printf("Not Sorted");
    }
    return 0;
}